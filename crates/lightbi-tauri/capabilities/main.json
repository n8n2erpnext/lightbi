{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lightbi-main-runtime",
  "description": "Allows the main LightBI webview to observe native desktop command events and open approved external URLs; window chrome remains OS/native-owned.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
