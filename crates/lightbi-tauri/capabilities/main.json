{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lightbi-main-runtime",
  "description": "Allows the main LightBI webview to observe native updater progress events without granting unrelated native capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
